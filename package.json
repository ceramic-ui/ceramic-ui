{
  "name": "ceramic-ui",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "lerna run build && rm -rf docs && mv packages/web/docs ."
  },
  "workspaces": ["packages/web"],
  "devDependencies": {
    "lerna": "^2.1.2"
  }
}
